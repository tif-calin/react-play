import React from 'react';

interface Props {
  domain: [number, number];
  range: [number, number];
};

const Axis: React.FC<Props> = ({ domain, range }) => {
  return (
    <div></div>
  );
};

export default Axis;
